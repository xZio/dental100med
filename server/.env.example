# DB_PATH задаётся только в проде (volume): DB_PATH=/data/app.db
JWT_SECRET=замени_на_случайную_строку_минимум_32_символа
PORT=5000
ADMIN_EMAIL=admin@dental100med.ru
ADMIN_PASSWORD=твой_пароль
ADMIN_PASSWORD_HASH=хэш_сгенерированный_через_seed.js
# Web Push (без них уведомления просто выключены)
# Свою пару сгенерировать: node -e "console.log(require('web-push').generateVAPIDKeys())"
VAPID_SUBJECT=mailto:admin@dental100med.ru
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
